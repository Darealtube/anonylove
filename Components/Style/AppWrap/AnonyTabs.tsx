import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const AnonyTabs = styled(Box)`
  bordercolor: divider;
  border-bottom: 8px;
  position: sticky;
  top: 90;
  background: transparent;
  z-index: 2;
`;
